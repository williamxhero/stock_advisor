# Ubiquitous Language

本词典是 `stock_advisor` 的统一业务语言入口，供产品、领域专家、开发者、测试和 AI 共同使用。根目录 `CONTEXT.md` 仍是术语定义的权威来源；本文件提炼其核心词汇、关系和歧义处理，不创建第二套业务语义。若两者冲突，以 `CONTEXT.md` 为准，并应在同一次变更中消除冲突。

## 原则代号

以下四字代号是 `docs/architecture/APP_DEVELOPMENT_PRINCIPLES.md` 中 canonical 原则的稳定简称，用于需求、设计、实现、测试和评审中的精确引用。代号不能脱离详细定义单独解释，也不能替代原原则文件；发生冲突时，以原原则文件为准。

### 应用原则

| 四字代号 | 正式原则 | 核心问题 |
| --- | --- | --- |
| **盈利为纲** | 目标与资格门 | 这项能力是否真实提升长期赚钱能力，并先排除毁灭性风险？ |
| **独立伴生** | 独立炒股搭档 | AI 是否像一个连续、独立且会表达分歧的专业伙伴？ |
| **消息不改** | 消息不可变 | 已经正式说出口的内容是否只追加、不回写？ |
| **命题分权** | 命题级事实权威 | 一段话中的每个命题由谁提供事实权威、由谁验证？ |
| **内外分层** | 内外分层 | 用户看到的是自然伙伴，还是暴露的内部机器过程？ |
| **秘密隔离** | 秘密与普通上下文 | 能授予控制权的秘密是否被阻断，普通投资上下文是否仍可按需使用？ |
| **风险守门** | AI 专业风险立场 | 高风险方案是否被明确分析但拒绝包装成合格执行建议？ |

#### 盈利为纲

**含义：** 产品对外只有一个目标：长期提升用户的炒股赚钱能力；对内只有真实、可执行、可重复且不承担毁灭性风险的收益，才算有效能力。

**开发约束：**

- 不能用点击率、回复速度、模型自评分或短期命中替代真实投资价值。
- 收益与毁灭性风险不是可以互相抵消的加权项；风险资格门不通过时，不得给出“可执行认可”。
- 新功能必须说明它改善了判断质量、执行质量、学习能力或风险控制中的哪一项，并保留可验证证据。
- 评测应保留速度、质量、结果和安全等独立维度，不得用单一总分掩盖退化。

#### 独立伴生

**含义：** AI 是有独立认知、知识、判断和自然人格的炒股搭档，不是服从式工具；对外始终是同一个连续伙伴，内部能力拆分不能表现为人格或角色切换。

**开发约束：**

- AI 必须认真处理用户观点，但市场事实、因果、预测、方法和风险仍由 AI 独立验证。
- AI 可以明确不同意用户，不得为了顺从而修改判断、方法或专业风险立场。
- 用户始终决定真实交易；系统记录用户行为事实，不伪造 AI 的赞同。
- M1 必须与 H0 原文、H0 命题和派生信号隔离；M2 才承担人机综合。
- 通用认知只理解一次用户表达，持仓、记忆和工作流模块只做确定性验证与执行，避免多个 AI 对同一句话给出竞争解释。

#### 消息不改

**含义：** 用户提交后的消息和 AI 首个可见片段出现后的消息都是已经说出口的历史，只能继续追加，不能编辑、删除、覆盖、替换或撤回。

**开发约束：**

- “发送”只进入可撤回的待提交区；“提交”才冻结不可变批次并触发处理。
- AI 流式输出只能追加尾部片段，不得重写用户已经看到的前缀。
- 错误、补充、反驳和变化必须用引用原消息的新消息表达。
- 流式失败时保留已显示内容并另行追加失败记录；恢复后继续处理原批次，不要求用户重输。
- 可以提供清空整个本地档案，但必须先允许导出并二次确认；不得借此支持选择性改写单条历史。

#### 命题分权

**含义：** 系统按一句话中可独立判断的命题分别确定事实权威、验证责任和处理结果，不把整段表达粗暴归类为个人事实或外部观点。

**开发约束：**

- 用户对自己的成交、持仓、资金、意图、偏好和其他私有事实具有事实权威。
- 用户对市场状态、外部事实、因果、预测、方法和风险不具有事实权威，相关内容必须独立核验。
- 同一句话可以同时产生用户事实和待验证外部主张，但拆分不得改变原意，原始表达必须保留。
- 一个命题不完整时只阻塞依赖它的写入或动作，其他明确命题仍可处理；未知值不得猜测补造。

#### 内外分层

**含义：** 对外是自然、连续的专业炒股伙伴；对内是为可靠判断服务的证据、状态、反证、快照、评测、审计和机器投影。

**开发约束：**

- 正常对话不展示模型名、token、工程分数、内部角色或结构化推理过程。
- 用户询问依据时，用自然语言解释关键原因、证据时点和反证，而不是倾倒内部日志。
- 重大证据过期或冲突足以改变建议时，AI 应主动说明。
- UI 可以展示用户做决定所需的状态和回执，但不得让内部实现概念取代业务语言。

#### 秘密隔离

**含义：** 只有泄露后会授予控制权、冒用权或造成实际资产与权限损失的信息才是必须阻断的认证秘密；普通投资上下文不是认证秘密，可按任务需要安全使用。

**开发约束：**

- 密码、API key、令牌、Cookie、会话凭证、私钥、MFA 验证码和恢复码不得进入模型、查询、记忆、日志、Exchange 或用户可见投影。
- 认证秘密必须在用户提交或 AI 片段首次显示前阻断，不能先持久化再事后遮盖。
- 持仓、成本、盈亏、市场观点、偏好、经验和计划属于普通投资上下文，可在可信接收方和既定权限内按任务相关性使用。
- “可以发送”不等于“每次全量发送”；上下文选择仍要减少无关噪声、成本和错误关联。
- 浏览器只用于信息获取，不得上传本地文件、发帖、私信、交易或修改外部账户状态。

#### 风险守门

**含义：** AI 维护独立、版本化的专业风险立场；可以分析高风险机会，但违反立场的方案不能被包装成合格执行建议。

**开发约束：**

- 默认不认可杠杆建议，单股不超过已知总资产 20%，同题材不超过 40%，单笔预设最大损失不超过总资产 1%。
- 已同步组合从峰值回撤 15% 后，不认可新增风险，应先复盘。
- 精确仓位建议要求持仓和总资产在最近一个交易日内更新；数据过期时只能给方向与条件，不能给精确股数或仓位。
- 用户可以作出不同决定，系统仍应如实记录事实；AI 不得把用户选择反写成自己的专业认可。

### 开发原则

| 四字代号 | 正式原则 | 核心问题 |
| --- | --- | --- |
| **产技分工** | 产品与技术决策分工 | 这是用户必须决定的产品取舍，还是 Codex 应直接承担的技术实现？ |
| **本机单写** | 本机运行时契约 | 业务事实是否只有本机运行时一个写入者？ |
| **确定先行** | 认知预算 | 能由确定性代码完成的问题是否错误地交给了 LLM？ |
| **证据进化** | 可控进化 | 策略和系统变化是否由冻结、跨周期证据支持并保持权限边界？ |

#### 产技分工

**含义：** 用户负责产品目标、业务语义、体验、权限、成本边界、不可逆约束和验收结果；Codex 负责架构、接口、数据流、可靠性、测试、迁移、性能和等价产品行为下的技术选择。

**开发约束：**

- 只有会改变用户可见结果、工作流、事实或权限归属、隐私安全、成本等待、可恢复性、兼容承诺或验收标准的选择，才升级为产品问题。
- 询问用户前执行反事实影响测试：更换可行实现后，用户能否感受到有意义差异，或权利、风险、成本与承诺是否改变。
- 如果差异只存在于内部实现，Codex 应依据 canonical 原则和当前实现直接决策，不把技术机制选择转嫁给用户。
- 用户提出的机制若不受支持、不安全、脆弱或明显劣于可行方案，应说明证据并采用更强实现，同时保持产品结果不变。

#### 本机单写

**含义：** 本机运行时是业务事实和策略状态的唯一写入者；桌面端通过版本化 Exchange 提交幂等命令、读取只读投影并接收回执。

**开发约束：**

- 桌面端不得直接读取或写入业务主库。
- `%LOCALAPPDATA%\AITradingCompanion\exchange` 是跨进程 interface，不是第二份事实源。
- 正式历史、持仓和工作流状态由运行时业务数据库拥有，Markdown 和 UI 都只是可重建投影。
- 认证凭证不得写入 Exchange、日志或模型上下文。
- 任何新增写路径都必须证明仍由运行时完成事实提交，并具有幂等、原子和可恢复语义。

#### 确定先行

**含义：** 确定性问题不购买概率性认知；代码负责可精确定义的机制，LLM 只负责搜索理解、判断、反证、自然表达和无法确定性解决的语义歧义。

**开发约束：**

- 触发、投递、读取、计算、去重、校验、持久化、时点检查和 UI 投影必须由代码完成。
- **认知上限** 与 **探索深度** 是不同维度；不能用低层能力的高 Effort 替代真正需要的高认知上限。
- 不得用提高 Effort 掩盖网络、数据、时点、上下文或未来信息错误。
- 认知预算不是用户消费硬配额，不设置每日金额、token 或检索次数硬上限来提前终止正常任务。
- 代码仍必须设置请求超时、循环终止、并发保护和故障熔断，这些是可靠性护栏，不是认知消费预算。

#### 证据进化

**含义：** AI 可以自由提出市场、方法和工作流假说，但正式规则、风险立场和自动化能力的变化必须以可追溯证据、冻结时点和明确权限为基础。

**开发约束：**

- 成功、失败、无结论和被替代的假说都要留痕，不能只保留胜者。
- 正式晋升必须跨周期、跨市场状态并校正多次尝试，不能因少量近期命中固化规则。
- 搜索顺序、问题模板、来源配比、反证顺序和记忆权重可在既定信息、时间、资源与权限范围内依据证据自行调整。
- 扩大产品权限、可信接收方、联网范围、付费方式、实质延迟或不可逆自动化，必须形成提案并由用户批准。
- 市场观点、经验权重和专业风险立场可以证据化升级，但任何认知升级都不自动获得新的操作权限。

### 原则之间的优先关系

- **盈利为纲** 定义目标，但必须同时通过 **风险守门** 和评测中的非劣约束，不能用潜在收益抵消毁灭性风险。
- **独立伴生** 定义伙伴关系，**命题分权** 决定伙伴应相信谁、核验什么，**内外分层** 决定如何自然地向用户表达。
- **消息不改** 保护已发布历史，**本机单写** 保护事实写入权；前者约束时间线，后者约束数据所有权。
- **秘密隔离** 优先于消息发布和模型调用：秘密必须在正式边界前阻断；普通投资上下文仍按 **命题分权** 和任务相关性处理。
- **产技分工** 决定谁作选择，**确定先行** 决定代码与 LLM 的职责，**证据进化** 决定已上线行为如何安全变化。
- 原则冲突不能通过折中命名掩盖；先保护秘密、事实权威、消息不可变和风险资格门，再在剩余可行方案中优化用户价值与技术质量。

## 伴生研判

| 术语 | 定义 | 应避免的别名 |
| --- | --- | --- |
| **研判周期** | 围绕一个正式时点组织 M0、H0、M1、M2、消息、草稿和持仓处理状态的完整业务上下文。 | 线程、报告、会话 |
| **当前判断** | 用户在今日列表中选中的 **研判周期**。 | 当前消息、当前线程、当前报告 |
| **M0（客观观察）** | AI 在明确时点形成、不含方向结论的客观信息讲述。 | 模型先验、隐藏判断、AI 结论 |
| **H0（我的判断）** | 用户在 M0 后且看到任何 AI 方向判断前锁定的自然语言判断包。 | 用户提示词、模型输入、单条消息 |
| **M1（独立 AI 判断）** | H0 锁定后，由不可见 H0 和后续聊天的 AI 独立形成的方向判断。 | 联合判断、H0 摘要、M0 揭示 |
| **M2（伴生综合）** | M1 完成后，综合 M0、冻结 H0 和 M1 形成的人机协同判断。 | 第二份 M1、覆盖结论、胜负裁决 |
| **判断修订** | 正式判断发布后因重要新证据追加、引用原判断但不覆盖原文与时点的判断。 | 重写、重新生成、静默修正 |
| **判断快照** | 判断发布时冻结的原文和可验证命题。 | 复盘摘要、当前观点 |

## 消息生命周期

| 术语 | 定义 | 应避免的别名 |
| --- | --- | --- |
| **AI 消息** | 属于当前判断的 AI 自然语言时间线，包含里程碑、聊天回复、动作回执、故障说明和判断修订。 | 报告区、模型输出表、M0/M1 面板 |
| **我的消息** | 属于当前判断的用户自然语言时间线。 | 我的判断栏、H0 列表 |
| **输入框草稿** | 尚未执行“发送”的可编辑本地文字。 | 待提交消息、H0 |
| **待提交消息** | 已执行“发送”、仍可撤回且尚未交给 AI 或业务处理的消息。 | 已提交消息、草稿文本 |
| **已提交批次** | 执行“提交 H0”或“提交”后冻结的一个或多个待提交消息。 | 待提交区、可编辑消息 |
| **发送** | 把输入框草稿移入待提交区而不触发 AI 或业务处理的动作。 | 提交、发布 |
| **提交** | 原子冻结当前待提交消息并把该批次交给 AI 和确定性业务处理的动作。 | 发送、保存草稿 |

## 证据、事实与时间

| 术语 | 定义 | 应避免的别名 |
| --- | --- | --- |
| **每日证据账本** | 盘前形成并在盘中追加、保留来源与时间口径的当日证据事实集。 | 一次性搜索结果、聊天记忆 |
| **发生时间** | 事件在现实中发生的时间。 | 获知时间、入库时间 |
| **获知时间** | 用户或系统实际获得信息、决定判断能否使用该信息的时间。 | 发生时间、文件修改时间 |
| **用户事实** | 由用户对自身成交、持仓、资金、意图和偏好作出的权威陈述。 | 市场事实、AI 结论 |
| **外部主张** | 关于市场、因果、预测、方法或风险且仍需 AI 独立验证的陈述。 | 已证实事实、用户事实 |
| **执行事件** | 运行时在任务生命周期中记录、只陈述已经发生事项的不可变事实。 | UI 埋点、评测结论、可覆盖状态 |

## 持仓

| 术语 | 定义 | 应避免的别名 |
| --- | --- | --- |
| **成交陈述（Trade Assertion）** | 用户关于已发生成交或当前真实持仓状态的原始自然语言陈述。 | 交易指令、下单请求 |
| **持仓变更草案（Portfolio Change Proposal）** | AI 从成交陈述中提取、保留原文证据且尚未成为事实的候选变更。 | 成交记录、模型决定 |
| **持仓成交事件（Portfolio Transaction）** | 通过确定性完整性与一致性校验后提交的真实成交事实。 | 建议、预测、信号 |
| **事实持仓（Portfolio Position）** | 由持仓基线和已提交持仓成交事件计算出的当前股票、股数和成本状态。 | AI 持仓判断、候选仓位 |
| **持仓快照（Portfolio Snapshot）** | 在明确时间口径下组合事实持仓和参考价格的只读视图。 | 券商实时账户、实时行情 |

## 调度、交付与评测

| 术语 | 定义 | 应避免的别名 |
| --- | --- | --- |
| **正式日程** | 定义未来正式任务时点的版本化产品规则，唯一权威来源是 `resources/schedules/tasks.json`。 | SQLite 日程源、临时覆盖、Codex 定时任务 |
| **日程修订** | 任务模板每次获准变更后形成、供未来周期冻结引用的不可变配置版本。 | 原地覆盖、当前运行参数漂移 |
| **计划启动时点** | 正式任务按冻结日程开始观察和处理的目标时间。 | 交付截止时间、预计发布时间 |
| **合格交付** | 某阶段结果首次通过自身质量资格门并正式发布的事实。 | 模型完成、任务结束、生成成功 |
| **主要合格交付** | 首先向用户兑现某项正式任务核心价值的阶段合格交付。 | 最终阶段、完整周期结束 |
| **有效交付窗口** | 某阶段结果仍能兑现其原定决策价值的时间范围。 | 模型超时、无限等待、恢复补偿窗口 |
| **资格判定** | 确定性资格门对阶段产物及证据作出的可追溯通过或拒绝事实。 | 评测分数、模型自评、发布成功 |
| **评测向量** | 同一任务或候选在速度、合格概率、研究质量、判断结果和安全可靠性上的并列评测结果。 | 综合评分、排行榜分数、单一目标 |
| **非劣约束** | 候选优化某一维度时，其他受保护维度不得出现实质退化的硬约束。 | 加权抵消、以快补差、平均后合格 |
| **交付时间预测** | 对合格交付真实时点给出的、带数据版本与不确定性的概率区间。 | 承诺时点、平均完成时间 |
| **窗口内合格概率** | 任务在有效交付窗口结束前首次合格并正式发布的概率。 | 成功率、平均交付时间 |

## 策略进化

| 术语 | 定义 | 应避免的别名 |
| --- | --- | --- |
| **进化候选** | 有评测证据支持、尚未成为生产规则的版本化策略方案。 | 已生效策略、评测结论、临时建议 |
| **冻结回放** | 使用历史时点可得且已冻结的事实重演候选策略的离线评测。 | 事后信息泄漏、历史拟合即晋升 |
| **实时配对影子** | 基线与候选在未来相同上下文中隔离运行、候选不向用户发布的配对实验。 | 替代正式结果、无配对前后比较 |
| **进化实验** | 在同一目标、分层和评测政策下比较版本化候选与冻结基线的生命周期。 | 单次运行、候选策略本身 |
| **证据成熟度** | 证据能否区分真实改进、随机波动与潜在退化的状态。 | 固定样本数、成功次数、观察天数 |
| **治理裁决** | 治理依据指定评测快照和版本化政策对候选作出的观察、淘汰、升级或回滚决定。 | LLM 意见、评测建议、策略写入 |
| **生产策略版本** | 经治理正式晋升、供后续任务执行且可追溯和回滚的策略快照。 | 当前统计值、未审批提案、临时配置 |
| **策略执行回执** | 策略所有者执行治理裁决后留下、证明策略是否实际改变的不可变结果。 | 治理裁决、评测建议、当前配置快照 |

## 认知预算

| 术语 | 定义 | 应避免的别名 |
| --- | --- | --- |
| **认知上限（Intellect）** | 某类任务所需的能力层级。 | 模型名、Provider、探索深度 |
| **探索深度（Effort）** | 一次认知任务在既定认知上限内投入的推演与检查深度。 | 固定 Provider 参数、认知上限、消费额度 |
| **认知策略版本** | 治理批准、用于选择探索深度的不可变规则集合。 | Provider 配置、单次路由决定、代码默认值 |
| **探索深度决定（EffortDecision）** | 任务开始前依据冻结任务特征和期限形成、供正式尝试引用的不可变选择。 | Provider 默认值、影子候选、模型路由 |

## 关系

- 一个 **当前判断** 恰好指向一个 **研判周期**；一个研判周期拥有零或多条 **AI 消息**、**我的消息** 和 **已提交批次**。
- 一个正式研判周期按 `M0 → H0 锁定 → M1 → M2（仅有 H0 时）` 演进；**M1** 不得读取 H0，**M2** 才能综合二者。
- **发送** 只产生 **待提交消息**；**提交** 才产生不可变的 **已提交批次** 并触发 AI 或业务处理。
- 一条用户表达可以同时包含 **用户事实** 与 **外部主张**；二者必须逐命题拆分，并采用不同的验证责任。
- 一条 **成交陈述** 可以产生零或多个 **持仓变更草案**；只有通过确定性校验的草案才能提交为 **持仓成交事件**。
- **事实持仓** 由持仓基线与全部有效的 **持仓成交事件** 推导；**持仓快照** 是带时间口径的只读投影，不是新的事实源。
- 一个正式任务引用一份冻结的 **日程修订**；恢复补偿窗口不得改变原 **计划启动时点** 或伪造 **合格交付**。
- 一个 **进化实验** 比较一个冻结基线和至少一个 **进化候选**；候选只有在证据成熟、满足 **非劣约束** 并获 **治理裁决** 后，才可成为 **生产策略版本**。
- **治理裁决** 不等于策略已经生效；只有成功的 **策略执行回执** 能证明生产策略发生了变化。

## 示例对话

> **开发者：**“用户点了‘发送’，我们现在可以把这些内容加入 H0 并启动 M1 吗？”
>
> **领域专家：**“不可以。‘发送’只产生 **待提交消息**；用户执行‘提交 H0’后才形成冻结的 **H0**，随后才能启动看不到 H0 的 **M1**。”
>
> **开发者：**“那 M1 完成后，系统把 H0 和 M1 合成一条新的独立判断？”
>
> **领域专家：**“合成结果叫 **M2（伴生综合）**，不是 M1。M1 保持独立，M2 才表达伙伴与用户的综合判断。”
>
> **开发者：**“如果用户后来补充了一笔成交呢？”
>
> **领域专家：**“先保留为 **成交陈述**，提取 **持仓变更草案**，通过确定性校验后提交 **持仓成交事件**；普通聊天不会静默改写原判断或事实持仓。”

## 标记的歧义

- “任务”同时可能指 **研判周期**、正式调度项或 Codex 开发任务。业务文档应明确写“研判周期”或“正式任务”；不得把 Codex task/thread 当作产品调度来源。
- “发送”和“提交”不是同义词：前者可撤回且不触发处理，后者冻结批次并触发处理。按钮、测试和事件名必须保留这一区分。
- “我的消息”不等于 **H0**：我的消息可以是资料、问题、成交事实或流程反馈，只有在 H0 阶段提交并冻结的判断包才是 H0。
- “AI 判断”容易混指 M1、M2 或判断修订。涉及独立性、输入边界或评测时必须使用精确阶段名。
- “事实”容易混指用户自身事实与市场外部主张。用户对自身成交和持仓具有事实权威，对市场状态和因果关系的看法仍须独立验证。
- “持仓”容易混指 **事实持仓**、**持仓快照**、候选仓位或券商实时账户。产品只把事件推导出的事实持仓视为业务事实，快照只是带时间口径的投影。
- “成功”容易混指模型返回、阶段完成、发布成功或 **合格交付**。评测与验收只在产物通过资格门并正式发布时使用“合格交付”。
- “日程”容易混指 `tasks.json`、数据库投影或运行时临时状态。只有 **正式日程** 定义未来任务，数据库内容只可作为可重建投影和冻结审计。
- “修订”不得表示覆盖历史。**判断修订**、**日程修订** 和反向持仓事件都通过追加新事实表达变化，原记录保持不可变。
- “实时”在持仓界面中容易造成错误承诺。**持仓快照** 必须展示价格与持仓的时间口径，不得称为券商实时账户。
