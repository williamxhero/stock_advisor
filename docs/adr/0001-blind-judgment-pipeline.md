# 使用盲 M1、伴生 M2和确定性编排

伴生研判采用客观 M0、用户 H0、对 H0完全不可见的独立 M1，以及有 H0时才生成的联合 M2。调度、上下文组装、状态和持久化由确定性代码承担；每个阶段通过小电脑 Provider Broker 的单次 `/v1/generate` 调用取得独立结果，Broker 负责实际模型与上游选择，应用不保留 Provider 消息链、工具续接或模型路由。这比延续单一长会话或让 M1直接综合 H0增加了少量显式阶段，但保护了人机判断的独立性、时点公平性和可验证性。
