# 分别计量实时等待与预取工作

09:00 盘前机会发现与 09:45 开盘异常发现保持独立任务身份，但 09:45 可以复用 09:00 形成的合格日内基线并进行增量核验；基线不合格时必须显式进入恢复流程。09:45 的用户等待耗时从该任务实际启动算到 M0 合格发布，同时评测中心另行归因它所复用的 09:00 工作量、数据年龄、质量、失败和重试。进化治理可以分别调整 09:00 的预取提前量与 09:45 的实时启动时点，但不能通过把工作前移、重复或隐藏在上游任务中制造虚假的实时提速。
